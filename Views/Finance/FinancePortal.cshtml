@model UserMgmtMVC.Models.UserAssignRoleViewModel

@{
    ViewBag.Title = "FinancePortal";
}

<h2>Please Login again to access the Finance Portal</h2>

<br />

@*<h4>@TempData["AppId"]</h4>*@

<br />
<br />


@Html.Partial("_LoginPartialView")














@*<h2 style="text-align:center">Login</h2>

<br />
@using (Html.BeginForm("FinanceLogin", "Home", FormMethod.Post))
{

    @Html.AntiForgeryToken();
    @Html.ValidationSummary(true);
    if (ViewBag.Message != null)
    {
        <div style="border:solid 1px red">
            @ViewBag.Message

        </div>
    }

    <div style="color:red">
        @ViewData["Error"]
    </div>
    <table align="center">
        <tr>
            <td>@Html.LabelFor(a => a.Username)</td>
            <td>@Html.TextBoxFor(a => a.Username)</td>
            <td>@Html.ValidationMessageFor(a => a.Username)</td>
        </tr>
        <tr></tr>
        <tr>
            <td>@Html.LabelFor(a => a.Password)</td>
            <td>@Html.TextBoxFor(a => a.Password)</td>
            <td>@Html.ValidationMessageFor(a => a.Password)</td>
        </tr>

        <tr>
            <td></td>
            <td>
                <input type="submit" value="Login" />
            </td>
        </tr>
    </table>
}*@

